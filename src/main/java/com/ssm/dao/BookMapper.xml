<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssm.dao.BookMapper">

    <!--增加一个Book-->
    <insert id="addBook" parameterType="Books">
        insert into library.books(book_id, book_title, author, publisher, publication_date, isbn, book_description, book_cover, category_name,book_quantity, book_price, created_time)
        values (#{book_id,jdbcType=INTEGER}, #{book_title,jdbcType=VARCHAR}, #{author,jdbcType=VARCHAR},#{publisher,jdbcType=VARCHAR},
                #{publication_date,jdbcType=DATE}, #{isbn,jdbcType=VARCHAR}, #{book_description,jdbcType=VARCHAR}, #{book_cover,jdbcType=VARCHAR}, #{category_name,jdbcType=VARCHAR}, #{book_quantity,jdbcType=INTEGER}, #{book_price,jdbcType=DECIMAL}, #{created_time,jdbcType=DATE})
    </insert>

    <!--根据id删除一个Book-->
    <delete id="deleteBookById" parameterType="int">
        delete from books
        where book_id = #{book_id,jdbcType=INTEGER}
    </delete>

    <!--更新Book-->
    <update id="updateBook" parameterType="Books">
        update library.books
        set book_title = #{book_title},book_quantity = #{book_quantity},book_description = #{book_description}
        where book_id = #{book_id}
    </update>

    <!--根据id查询,返回一个Book-->
    <select id="queryBookById" resultType="Books">
        select * from library.books
        where book_id = #{book_id}
    </select>

    <!--根据id查询,返回一个Book-->
    <select id="queryBookByTitle" resultType="Books">
        select * from library.books
        where book_title LIKE concat('%',#{book_title,jdbcType=VARCHAR},'%')
    </select>

    <!--查询全部Book-->
    <select id="queryAllBook" resultType="Books">
        SELECT * from library.books
    </select>




</mapper>